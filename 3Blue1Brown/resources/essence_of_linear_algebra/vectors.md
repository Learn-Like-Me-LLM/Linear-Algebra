## VECTORS

> ![NOTE]  
> $\vec{v} = \begin{bmatrix} x \\ y \\ z \end{bmatrix}$

- ever pair of numbers represents 1 and only 1 vector and every vector is made up of 1 and only 1 pair of numbers
    - in 3+ dimensions this holds 1:1


### Perspective(s)

#### Physics
> a vector is an arrow pointing in space that is defined by the arrow's length and direction that it is pointing

#### Computer Science
> a vector is an ordered list of numbers

### Questions

1. How to differentiate between a "vector" and a "point"
---
a vector is usually written vertically with square brackets "[]" around them while a point is written horizontally with smooth brackets "()" around them.


$\vec{v} = \begin{bmatrix} -2 \\ 5 \end{bmatrix}$
-- vs --
$P = (-2, 5)$

### Vector Addition

> to add vectors take the tail of the vector2 and put it at the end of vector1. the NEW vector from the origin or vector1 to the end of vector2 is the result of the vector addition.

### Example
$V = (1, 2) - W = (3, -1)$

$\vec{v} \begin{bmatrix} 1 \\ 2 \end{bmatrix} + \vec{w}\begin{bmatrix} 3 \\ -1 \end{bmatrix} = \vec{vw}\begin{bmatrix} 1 + 3 \\ 2 + (-1) \end{bmatrix} = \vec{vw}\begin{bmatrix} 4 \\ 3 \end{bmatrix}$

### Vector Multiplication

> if you multiply a vector by a number you are "stretching" the length of that vector by that number

> if the number you are multiplying the vector by is negative then the vector is flipped 180 degrees before it is stretched

$\begin{bmatrix} 9 \\ 3 \end{bmatrix} * \frac{1}{3} = \begin{bmatrix} 3 \\ 1 \end{bmatrix}$

$\begin{bmatrix} 2 \\ -3 \end{bmatrix} * 2 = \begin{bmatrix} 4 \\ -6 \end{bmatrix}$

